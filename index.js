'use strict';

const { CloudEvent } = require('cloudevents');

// a function that takes the github hook and returns a valid cloudevent object
function parseGithubWebHook (webhook) {
  // convert the webhook

  // return a cloudevent object
}

module.exports = parseGithubWebHook;
